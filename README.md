# mimic-trick
A digital implementation of Mimic Trick playable in the console

To run it install a Lisp interpreter like SBCL or CLISP, load the main file and either call `(start-3p-game)` or `(start-4p-game)` to play.
